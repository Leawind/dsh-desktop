# 更新日志

## [0.2.1]

### 新增

- 添加托盘图标，可新建窗口与退出
- 新增原生应用更新能力，可检查并安装应用与内置运行时更新
- 内置运行时升级到最新已验证的 DSH 版本

### 修复

- 窗口标题与启动页拖拽行为更稳定，优化启动失败记录展示
- 修复内置运行时升级时依赖构建脚本被 pnpm 忽略导致的安装失败

## [0.2.0]

- 迁移到 WebUI
- 仅提供可独立移动 `bundled`、`slim` 单文件可执行程序
- 将设置入口、端点编辑和连接状态整合到窗口底部状态栏
- 支持跟随系统外观设置，并改进 Ubuntu 等 Linux 环境下的深色主题识别
- 改用浏览器原生缩放

## [0.1.1]

- 改进 UI

## [0.1.0]

- 提供 Windows、Linux 和 macOS 桌面应用。
- 提供内置 Node.js 与 DSH 运行环境的 `bundled` 安装包，以及使用外部 DSH 的 `slim` 安装包。
- 支持连接已有 DSH 服务、按端口范围启动受管服务，以及在多个窗口间复用服务。
- 提供当前窗口、全局设置、运行时状态和应用信息界面。
- 支持简体中文和英语（美国）界面。
