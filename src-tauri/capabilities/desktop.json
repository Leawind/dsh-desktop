{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop",
  "description": "Permissions for the trusted DSH Desktop top-level window.",
  "windows": ["main", "dsh-*"],
  "permissions": ["core:default", "core:window:default"]
}
