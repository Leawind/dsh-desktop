{
  "name": "@dsh-desktop/desktop",
  "private": true,
  "scripts": {
    "dev:bundled": "pnpm --workspace-root run runtime:prepare && pnpm --workspace-root run frontend:build && cross-env DSH_DESKTOP_VARIANT=bundled cargo run --manifest-path Cargo.toml",
    "dev:slim": "pnpm --workspace-root run frontend:build && cross-env DSH_DESKTOP_VARIANT=slim cargo run --manifest-path Cargo.toml"
  },
  "dependencies": {
    "@dsh-desktop/frontend": "workspace:*"
  }
}
